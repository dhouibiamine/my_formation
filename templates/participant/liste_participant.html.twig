
{% extends 'base.html.twig' %}

{% block title %}my_academic_project{% endblock %}

{% block body %}

 <body style="background-color: yellow;"> 



   <div class="container">
  
        
        <h1 class="my-4" style="color: red;">Liste des Participants</h1>
        <a href="{{ path('add_participant', {'formationId': formation.id}) }}" class="btn btn-secondary">ajouter participant</a>

          <a href="{{ path('liste_formation')  }}" class="btn btn-info">Liste des Formation</a>
         

          
          


        <table class="table">
            <thead>
                <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Email</th>
                    <th scope="col">Abonne</th>
                    <th scope="col">Fonction</th>


                    <th scope="col">Action</th>
                </tr>
            </thead>
            <tbody>
                {% for participant in lesparticipants %}
                    <tr>
                        <td>{{ participant.nom }}</td>
                        <td>{{ participant.email }}</td>
                        <td>{{ participant.isIsSubscribe() }}</td>
                        <td>{{ participant.fonction }}</td>
                        <td>
                            
                            
                           
                            
 <a href="{{ path('edit_participant', {'id': participant.id,'formationId': participant.formation.id}) }}" class="btn btn-primary">Edit</a>






 <a class="btn btn-danger" href="{{ path('can_delete', {'id':participant.id }) }}"
                     onclick="return confirm('Are you sure to delete {{ participant.nom}}')">Delete</a>
                           

                            



                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>

    </div>
    

{% endblock %}
